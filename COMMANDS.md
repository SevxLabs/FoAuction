# Commands

This change adds no commands. Existing `/auction` (`/ah`, `/foauction`) and
`/foauctionadmin` behavior remains defined in `src/main/resources/plugin.yml`.
Item maintenance is a server-side Java API for trusted installed plugins.
