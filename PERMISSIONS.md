# Permissions

This change adds no player permissions. Existing command permissions remain in
`src/main/resources/plugin.yml`. The stored-item API is available to trusted
server plugins; it is not exposed to players, plugin messages or network input.
